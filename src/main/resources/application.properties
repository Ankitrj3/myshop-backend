spring.application.name=myapp
spring.data.mongodb.uri=mongodb+srv://${DB_USERNAME:ankitrobinranjan}:${DB_PASSWORD:15aGG15}@cluster0.wsgx63d.mongodb.net/myapp?retryWrites=true&w=majority&appName=Cluster0
server.port=${SERVER_PORT:8080}

# Security configuration
# spring.security.user.password=${ADMIN_PASSWORD:admin}

# Additional environment-based configuration
spring.profiles.active=${SPRING_PROFILES_ACTIVE:dev}